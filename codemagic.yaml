workflows:
android-apk:
name: RAVE Android APK

```
environment:
  node: 20

scripts:
  - npm install
  - npm run build
  - npx cap sync android

  - cd android
  - ./gradlew assembleDebug

artifacts:
  - android/app/build/outputs/apk/debug/*.apk
```
